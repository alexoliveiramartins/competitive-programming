#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int l, r;
    cin >> l >> r;
    if(l == r){
        cout << "Invalid";
        return 0;
    }
    l == 1 ? cout << "Yes" : cout << "No"; 

}